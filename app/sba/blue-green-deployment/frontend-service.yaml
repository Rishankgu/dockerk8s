apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
    release: v1
  name: frontend 
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: frontend
  type: ClusterIP